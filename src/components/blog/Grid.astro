---
import Item from '~/components/blog/GridItem.astro';

const { posts } = Astro.props;
---

<div class="grid gap-6 row-gap-5 -mb-6 md:grid-cols-2 lg:grid-cols-3">
	{posts.map((post: any) => <Item post={post} />)}
</div>
